<h1>Forgot Password?</h1>

reset your password
<%= form_with model: @post, url: password_reset_edit_path(token: params[:token]) do |form| %>
  <div>
    <%= form.label :password %>
    <%= form.password_field :password %>
  </div>
  <div>
    <%= form.label :password_confirmation %>
    <%= form.password_field :password_confirmation %>
  </div>
  <div>
    <%= form.submit "Reset" %>
  </div>
<% end %>
